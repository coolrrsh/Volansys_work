

::Streaming::

gst-launch-1.0 v4l2src device=/dev/video1 ! video/x-h264,width=1920,height=1080,framerate=30/1 ! h264parse ! rtph264pay name=pay0 pt=96 ! udpsink host=127.0.0.1 port=5000

::Recording::

gst-launch-1.0 -v udpsrc address=127.0.0.1 port=5000 ! "application/x-rtp, media=(string)video, clock-rate=(int)90000, encoding-name=(string)H264, payload=(int)96" ! rtph264depay ! h264parse ! avdec_h264 ! videoconvert ! autovideosink sync=false


